{
  "name": "smart-timetable",
  "version": "1.0.0",
  "description": "Smart Timetable Scheduling System",
  "scripts": {
    "dev": "concurrently \"cd backend && uvicorn main:app --reload --host 0.0.0.0 --port 8000\" \"cd frontend && npm run dev\"",
    "install:all": "cd frontend && npm install && cd ../backend && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
